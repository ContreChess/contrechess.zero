define(function (require) {
  var $         = require('jquery'),
      Backbone  = require('backbone'),
      self,

      game = Backbone.Model.extend({
      //address: '',
      //
      //userName: '',
      //authAddress: ''
      });
});
